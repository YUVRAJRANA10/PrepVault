# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/prepvault
# For MongoDB Atlas (cloud), use: mongodb+srv://username:password@cluster.mongodb.net/prepvault

# Server Port
PORT=5000

# JWT Secret Key (change this to something more secure in production)
JWT_SECRET=prepvault_secret_key_2026

# Node Environment
NODE_ENV=development
